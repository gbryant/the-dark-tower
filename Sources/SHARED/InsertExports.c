Copyright 2016 Gregory BryantLicensed under the Apache License, Version 2.0 (the "License");you may not use this file except in compliance with the License.You may obtain a copy of the License at    http://www.apache.org/licenses/LICENSE-2.0Unless required by applicable law or agreed to in writing, softwaredistributed under the License is distributed on an "AS IS" BASIS,WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.See the License for the specific language governing permissions andlimitations under the License./***********************************************************************/#include "ProjectHeaders.h"void InsertExports(LabelList** topLabelList, CodeExport* codeExport){LabelList				*curr,*temp;CodeExport				*currExp;long					i,j;currExp = codeExport;curr = (*topLabelList);if(!curr){curr = (LabelList*)NewPtr(sizeof(LabelList));curr->index = currExp->offset;curr->next 			= 0;curr->prev			= 0;curr->label = NewPtr(currExp->label[0]+1);MyPStrAdd((unsigned char*)currExp->label,"\p",(unsigned char*)curr->label);currExp = currExp->next;}while(currExp){		if(currExp->offset<curr->index)	{		while(curr->prev)		{			curr = curr->prev;			if(currExp->offset>curr->index||currExp->offset==curr->index)			{break;}		}	}	else if(currExp->offset>=curr->index)	{		while(curr->next)		{			curr = curr->next;			if(currExp->offset<curr->index||currExp->offset==curr->index)			{break;}		}	}	else{break;}	if(currExp->offset>=curr->index)	{gLabelListSize++;		temp = (LabelList*)NewPtr(sizeof(LabelList));		temp->index = currExp->offset;		temp->next 			= curr->next;		temp->prev			= curr;		temp->label = NewPtr(currExp->label[0]+1);		MyPStrAdd("\p@Export ",(unsigned char*)currExp->label,(unsigned char*)temp->label);		if(curr->next)		{curr->next->prev	= temp;}		curr->next 			= temp;		curr=temp;	}	//else if(currExp->offset<curr->index)	else	{gLabelListSize++;		temp = (LabelList*)NewPtr(sizeof(LabelList));		temp->index = currExp->offset;		temp->next 			= curr;		temp->prev 			= curr->prev;		temp->label = NewPtr(currExp->label[0]+1);		MyPStrAdd("\p@Export ",(unsigned char*)currExp->label,(unsigned char*)temp->label);		if(curr->prev)		{curr->prev->next 	= temp;}		curr->prev 			= temp;		curr=temp;	}	currExp = currExp->next;}/*currExp = codeExport;while(currExp){	currLabelList = (*topLabelList);	prevLabelList = currLabelList;	if(!(*topLabelList))	{	(*topLabelList) = (LabelList*)NewPtr(sizeof(LabelList));	(*topLabelList)->index = (currExp->offset/4);	i=0;while(currExp->label[i]!=0){i++;}	(*topLabelList)->label = NewPtr(i+1+8);	for(j=0;j<8;j++){(*topLabelList)->label[j]="@Export "[j];}	for(j=0;j<i+1;j++){((*topLabelList)->label+8)[j]=currExp->label[j];}	(*topLabelList)->next = nil;	}	else	{		while((currExp->offset/4) > currLabelList->index)		{		if(!currLabelList->next){prevLabelList = currLabelList;break;}		prevLabelList = currLabelList;		currLabelList = currLabelList->next;		}		if(currLabelList == (*topLabelList) && (*topLabelList)->next)		{		__temp = (LabelList*)NewPtr(sizeof(LabelList));		__temp->index = (*topLabelList)->index;		__temp->label = (*topLabelList)->label;		__temp->next = (*topLabelList)->next;		(*topLabelList)->index = (currExp->offset/4);		i=0;while(currExp->label[i]!=0){i++;}		(*topLabelList)->label = NewPtr(i+1+8);		for(j=0;j<8;j++){(*topLabelList)->label[j]="@Export "[j];}		for(j=0;j<i+1;j++){((*topLabelList)->label+8)[j]=currExp->label[j];}		(*topLabelList)->next = __temp;		}		else if(currLabelList == (*topLabelList) && !(*topLabelList)->next)		{		__temp = (*topLabelList)->next;		(*topLabelList)->next = (LabelList*)NewPtr(sizeof(LabelList));		(*topLabelList)->next->index = (currExp->offset/4);		i=0;while(currExp->label[i]!=0){i++;}		(*topLabelList)->next->label = NewPtr(i+1+8);		for(j=0;j<8;j++){(*topLabelList)->next->label[j]="@Export "[j];}		for(j=0;j<i+1;j++){((*topLabelList)->next->label+8)[j]=currExp->label[j];}		(*topLabelList)->next->next = __temp;		}		else		{		__temp = prevLabelList->next;		prevLabelList->next = (LabelList*)NewPtr(sizeof(LabelList));		prevLabelList->next->index = (currExp->offset/4);		i=0;while(currExp->label[i]!=0){i++;}		prevLabelList->next->label = NewPtr(i+1+8);		for(j=0;j<8;j++){prevLabelList->next->label[j]="@Export "[j];}		for(j=0;j<i+1;j++){(prevLabelList->next->label+8)[j]=currExp->label[j];}			prevLabelList->next->next = __temp;		}	}currExp = currExp->next;}*/}